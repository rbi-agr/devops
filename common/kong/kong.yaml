# a very minimal declarative config file
_format_version: "3.0"

plugins:
  - name: prometheus
    enabled: true
    config:
      per_consumer: true
      status_code_metrics: true
      latency_metrics: true
      bandwidth_metrics: true
      upstream_health_metrics: true

# services:
  # - name: bff
  #   url: http://bff:3000
  #   routes:
  #   - name: bff_route
  #     expression: 'http.path ^= "/bff" '


    